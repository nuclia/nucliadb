apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: ingest-pinecone-consumer
  labels:
    app: ingest-pinecone-consumer
    version: "{{ .Chart.Version | replace "+" "_" }}"
    chart: "{{ .Chart.Name }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: ingest-pinecone-consumer
  minReplicas: {{.Values.ingest_pinecone_consumer_autoscaling.minReplicas}}
  maxReplicas: {{.Values.ingest_pinecone_consumer_autoscaling.maxReplicas}}
  metrics: {{- toYaml .Values.ingest_pinecone_consumer_autoscaling.metrics | nindent 4}}
