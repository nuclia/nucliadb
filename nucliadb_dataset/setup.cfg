[zest.releaser]
tag-format = nucliadb_dataset-{version}
tag-message = NucliaDB Dataset {version}

[tool:pytest]
# pytest is requiring this for async projects otherwise
# we'd need to change all our fixtures
asyncio_mode=auto
