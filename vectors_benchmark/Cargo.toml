[package]
name = "vectors_benchmark"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
tempfile = "3"
clap = { version = "3.1.18", features = ["derive"] }
nucliadb_vectors2 = {path= "../nucliadb_vectors2"}