{{- range $i := untilInt .Values.replicaCount }}
apiVersion: v1
kind: Service
metadata:
  name: node-replica-{{ $i }}
  labels:
    app: node-replica-{{ $i }}
    appType: node
    app.kubernetes.io/name: node-replica-{{ $i }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    version: "{{ .Chart.Version | replace "+" "_" }}"
    chart: "{{ .Chart.Name }}"
spec:
  selector:
    app: node-replica-{{ $i }}
    chart: "{{ .Chart.Name }}"
  clusterIP: None
  ports:
  - name: grpc-reader
    port: {{ .Values.serving.grpc_reader }}
    protocol: TCP
    appProtocol: grpc
  - name: grpc-writer
    port: {{ .Values.serving.grpc_writer }}
    protocol: TCP
    appProtocol: grpc
{{- end }}