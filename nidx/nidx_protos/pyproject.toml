[build-system]
requires = ["pdm-backend", "grpcio-tools"]
build-backend = "pdm.backend"

[project]
name = "nidx_protos"
dynamic = ["version"]

[tool.pdm.build]
custom-hook = "build.py"

[tool.pdm.version]
source = "call"
getter = "build:get_version"
