[package]
name = "nidx_tests"
version = "0.1.0"
edition = "2024"
license = "Apache-2.0"

[dependencies]
nidx_protos = { version = "0.1.0", path = "../nidx_protos" }
uuid = { version = "1.11.0", features = ["v4"] }
