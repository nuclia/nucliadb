name: nucliadb Reader (py)

on:
  pull_request:
    branches:
      - main
    paths:
      - ".github/workflows/nucliadb_reader.yml"
      - "charts/nucliadb_reader/**"
      - "nucliadb_reader/**"
      - "nucliadb_models/**"
      - "nucliadb_utils/**"
      - "nucliadb_ingest/**"
      - "nucliadb_models/**"
      - "Dockerfile.reader"
  push:
    branches:
      - main
    paths:
      - ".github/workflows/nucliadb_reader.yml"
      - "charts/nucliadb_reader/**"
      - "nucliadb_reader/**"
      - "nucliadb_models/**"
      - "nucliadb_ingest/**"
      - "nucliadb_models/**"
      - "Dockerfile.reader"

env:
  API_VERSION: 1
  COMPONENT: nucliadb-reader
  HASH: $(git rev-parse --short "$GITHUB_SHA")
  BRANCH: ${GITHUB_REF##*/}
  CONTAINER_REGISTRY: eu.gcr.io/${{ secrets.GCP_PROJECT_ID }}
  IMAGE_NAME: reader

jobs:
  # Job to run pre-checks
  pre-checks:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: [3.9]

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3.1.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install package
        run: |
          pip install -r code-requirements.txt
      - name: Run pre-checks
        run: |
          flake8 nucliadb_reader --config=nucliadb_reader/setup.cfg
          isort -c --profile black nucliadb_reader/
          black --check --verbose nucliadb_reader

  # Job to run tests
  tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: [3.9]

    steps:
      - name: Checkout the repository
        uses: actions/checkout@v3

      - name: Setup Python
        uses: actions/setup-python@v3.1.0
        with:
          python-version: ${{ matrix.python-version }}
          cache: "pip"

      - name: Install the package
        run: |
          pip install -r test-requirements.txt
          pip install -r nucliadb_reader/requirements-sources.txt
          pip install -e nucliadb_swim
          pip install -e nucliadb_cluster
          pip install -r nucliadb_ingest/requirements.txt
          pip install -e nucliadb_ingest
          pip install -r nucliadb_reader/requirements.txt
          pip install -e nucliadb_reader

      - name: Check mypy
        run: |
          MYPYPATH=./mypy_stubs mypy nucliadb_reader/

      - name: Run tests
        run: |
          pytest -rfE --cov=nucliadb_reader -s --tb=native -v --cov-report xml --cov-append nucliadb_reader/nucliadb_reader

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v3
        with:
          file: ./coverage.xml

      - name: Extract docs
        run: |
          mkdir -p /tmp/openapi
          extract-openapi /tmp/openapi/$COMPONENT.json $API_VERSION

      - name: Cache extracted docs
        uses: actions/cache@v3.0.2
        with:
          path: /tmp/openapi
          key: openapi-${{ env.COMPONENT }}

  build:
    name: Build image and push
    runs-on: ubuntu-latest
    needs: tests
    if: github.event_name == 'push'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      # Setup gcloud CLI
      - uses: google-github-actions/auth@v0
        with:
          credentials_json: "${{ secrets.GCP_CREDENTIALS }}"

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v0"

      - name: Configure Docker
        run: gcloud auth configure-docker --quiet

      # Build docker image
      - name: Image
        run: |-
          docker build -t $CONTAINER_REGISTRY/$IMAGE_NAME:$HASH . -f Dockerfile.reader
      - name: Tag
        run: |-
          docker tag $CONTAINER_REGISTRY/$IMAGE_NAME:$HASH $CONTAINER_REGISTRY/$IMAGE_NAME:$BRANCH
      - name: Push sha
        if: github.event_name == 'push'
        run: |-
          docker push $CONTAINER_REGISTRY/$IMAGE_NAME:$HASH
      - name: Push latest
        if: github.event_name == 'push'
        run: |-
          docker push $CONTAINER_REGISTRY/$IMAGE_NAME:$BRANCH

  deploy:
    name: Deploy Helm chart and trigger internal CI
    runs-on: ubuntu-latest
    needs: build
    if: github.event_name == 'push'

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set helm package image
        id: version_step
        run: |-
          sed -i.bak "s#IMAGE_TO_REPLACE#$IMAGE_NAME:$HASH#" ./charts/nucliadb_reader/values.yaml
          sed -i.bak "s#CONTAINER_REGISTRY_TO_REPLACE#$CONTAINER_REGISTRY#" ./charts/nucliadb_reader/values.yaml
          VERSION=`cat nucliadb_reader/VERSION`
          VERSION_SHA=$VERSION+$HASH
          sed -i.bak "s#99999.99999.99999#$VERSION_SHA#" ./charts/nucliadb_reader/Chart.yaml
          echo "::set-output name=version_number::$VERSION_SHA"

      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"

      - name: Install Helm
        uses: azure/setup-helm@v2.0
        with:
          version: v3.4.0

      - name: Push helm package
        run: |-
          helm lint charts/nucliadb_reader
          helm package charts/nucliadb_reader
          curl --data-binary "@nucliadb_reader-${{ steps.version_step.outputs.version_number }}.tgz" ${{ secrets.HELM_CHART_URL }}/api/charts

      - name: Trigger internal Jenkins
        run: |-
          curl -u "${{ secrets.JENKINS_API_USER }}:${{ secrets.JENKINS_API_KEY }}" --data-urlencode "NUCLIADB_COMPONENT=reader"  --data-urlencode "VERSION=${{ steps.version_step.outputs.version_number }}" ${{ secrets.JENKINS_WEBHOOK_URL }}

  docs:
    name: "Upload API specs to GCS"
    runs-on: ubuntu-latest
    needs: deploy
    if: github.event_name == 'push'

    steps:
      - name: Cache extracted docs
        uses: actions/cache@v3.0.2
        with:
          path: /tmp/openapi
          key: openapi-${{ env.COMPONENT }}

      # Setup gcloud CLI
      - uses: google-github-actions/auth@v0
        with:
          credentials_json: "${{ secrets.GCP_CREDENTIALS }}"

      - name: "Set up Cloud SDK"
        uses: "google-github-actions/setup-gcloud@v0"

      - name: Upload docs
        run: |
          gsutil copy /tmp/openapi/$COMPONENT.json gs://stashify-docs/api/regional/v$API_VERSION/$COMPONENT/spec.json
