[build-system]
requires = ["pdm-backend", "grpcio-tools @ file:///tmp/grpcio_tools-1.67.1"]
build-backend = "pdm.backend"

[project]
name = "nidx_protos"
version = "0.0.1"

[tool.pdm.build]
custom-hook = "build.py"
